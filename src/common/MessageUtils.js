// @flow strict-local

import * as Logger from "./Logger";
import * as Dates from "./Date";
import { Alert } from "react-native";
import RootMessage from "./Message";

export type X = {
  // id: string,
  // uid: string,
  // timestamp: number,
  // lastUpdated: number,
  // children: Array<Message>,
  // event_poll_response: {[key: string]: mixed},
  // event_poll: {[key: string]: mixed},
  // user: UserInfo,
};
